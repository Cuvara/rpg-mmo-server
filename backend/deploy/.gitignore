# Local dev secrets and build output
.env
# Generated by `./stack.sh up --scratch`: .env with the isolated stack's ports
# layered on top. Regenerated on every scratch `up`.
.env.scratch
modules/*.so
